= render 'scripts/tiny_mce'

- page_title!('Editando proyecto')

#region-proyectos.container

  .region
    .region-title
      %h1
        Editando
        %b
          proyecto

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @proyecto, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
