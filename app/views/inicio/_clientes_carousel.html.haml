
#carousel-clientes.carousel.slide

  %ol.carousel-indicators
    - @clientes.each_with_index do |cliente, index|
      - if index == 0
        %li.active{:'data-target' => '#carousel-clientes', :'data-slide-to' => index}
      - else
        %li{:'data-target' => '#carousel-clientes', :'data-slide-to' => index}

  .carousel-inner
    - @clientes.each_slice(6).each_with_index do |cliente, index|
      - if index == 0
        .item.active
          - cliente.each do |cliente|
            .slide
              = link_to image_tag(cliente.logo_url(:thumb)), cliente.link, target: '_blank'
              = admin_controls('Editar', edit_cliente_path(cliente), 'down')       
              - if current_admin
                = link_to 'Eliminar', cliente, :method => :delete, :data => { :confirm => '¿Estas Seguro?' }


      - else
        .item
          - cliente.each do |cliente|
            .slide
              = link_to image_tag(cliente.logo_url(:thumb)), cliente.link, target: '_blank'
              = admin_controls('Editar', edit_cliente_path(cliente), 'down')       
              - if current_admin
                = link_to 'Eliminar', cliente, :method => :delete, :data => { :confirm => '¿Estas Seguro?' }       
      

  = link_to content_tag(:span, 'left', class: 'icon-prev'), '#carousel-clientes', :'data-slide' => 'prev'
  = link_to content_tag(:span, 'right', class: 'icon-next'), '#carousel-clientes', :'data-slide' => 'next'

