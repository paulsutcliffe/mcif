- page_title!('Editando asociado')

#region-asociados.container

  .region
    .region-title
      %h1
        Editando
        %b
          asociado

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @asociado, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
