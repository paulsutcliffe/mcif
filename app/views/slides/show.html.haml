#region-slides.container
  .region
    .region-title
      %h1= page_title!('slide')

  .region
    %p
      %b Imagen:
      = @slide.imagen
    %p
      %b Link:
      = @slide.link

  .region
    - if current_admin
      .admin-controls
        = link_to 'Editar', edit_slide_path(@slide), class: 'btn btn-success'
    = link_to 'Regresar', slides_path, class: 'btn btn-warning'
