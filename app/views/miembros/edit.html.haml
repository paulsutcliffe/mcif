= render 'scripts/tiny_mce'

- page_title!('Editando miembro')

#region-miembros.container

  .region
    .region-title
      %h1
        Editando
        %b
          miembro

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @miembro, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
